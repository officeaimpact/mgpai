# Углублённый анализ тестовой сессии 10 февраля 2026 (19:44–20:00)

**Модель:** `yandexgpt/rc`  
**Логи:** `dialogue_20260210_194450.md` + `server_20260210_194450.log`  
**Всего сессий:** 7  
**Всего сообщений пользователя:** 16  
**Всего вызовов Yandex GPT API:** 44  
**Всего вызовов TourVisor API:** 24  

---

## 1. СВОДКА ПО СЕССИЯМ

| # | Session ID | Сценарий | Результат | API calls | Latency | Карточек |
|---|-----------|----------|-----------|-----------|---------|----------|
| 1 | `6dc27fd5` | Базовый каскад (Турция) | **PASS** | 9 YGP + 4 TV | 13.7s | 5 |
| 2 | `0d16df4b` | Всё в одном (Египет, "пять звезд, все включено") | **PARTIAL FAIL** | 7 YGP + 4 TV | 18.0s | 5 |
| 3 | `48966250` | Горящие туры | **PASS** | 2 YGP + 1 TV | 4.0s | 7 |
| 4 | `321bccff` | Сочи без перелёта | **CRITICAL FAIL** | 2 YGP + 0 TV | 4.4s | 0 |
| 5 | `b7f59db9` | Кемер, конкретный курорт | **PASS** | 5 YGP + 3 TV | 10.8s | 5 |
| 6 | `99931e71` | Египет, "четыре звезды" + рейсы | **PARTIAL FAIL** | 9 YGP + 6 TV | 36.2s | 5 |
| 7 | `72180ccd` | Стресс-тест (4 функции) | **PASS** | 10 YGP + 6 TV | 26.7s | 17 |

**Итого:** 4 из 7 пройдены чисто, 2 частично провалены, 1 критически провален.

---

## 2. ДЕТАЛЬНЫЙ РАЗБОР КАЖДОГО ПРОБЛЕМНОГО СЦЕНАРИЯ

### 2.1 ПРОБЛЕМА P1: Ложное срабатывание каскадной валидации на "пять звезд, все включено"

**Сессия 2 (`0d16df4b`)**

Пользователь написал ВСЁ в одном сообщении:
> «Подбери тур в Египет из Санкт-Петербурга, вылет 15 марта, на 10 ночей, двое взрослых и ребенок 5 лет, **пять звезд, все включено**»

**Что произошло:**
1. Модель правильно разобрала запрос → вызвала `get_current_date()` → затем `search_tours(stars: 5, meal: 7)`
2. Но `_check_cascade_slots()` **заблокировала** вызов с ошибкой: "Клиент НЕ указал категорию отеля и тип питания"
3. Модель вынуждена переспросить: «Вы указали пять звёзд и всё включено, верно?»
4. Пользователь ответил «да» → поиск прошёл

**Корневая причина — 2 бага в regex `_check_cascade_slots()`:**

**Баг A: `stars_patterns` не распознаёт словесные формы числительных**

```python
# Текущий код (строки 204-207):
stars_patterns = [
    r'\d\s*(?:звёзд|звезд|\*|⭐)',              # "5 звёзд", "4*"
    r'(?:пяти|четырёх|четырех|трёх|трех)звёзд',  # "пятизвёздочный"
]
```

Паттерн `\d` требует ЦИФРУ. Слово «пять» не содержит цифры → **не матчит**.
Второй паттерн ожидает СЛИТНОЕ написание (`пятизвёздочный`), а не «пять звезд» с пробелом → **не матчит**.

**Баг B: `meal_patterns` не распознаёт "все" (с буквой "е" вместо "ё")**

```python
# Текущий код (строка 209):
r'(?:всё?\s*включен|all\s*incl|[ауа]и|ai\b|uai\b)'
```

Конструкция `всё?` разбирается regex-движком как: символы `в`, `с`, затем **опциональный** `ё`.  
Это матчит `"вс"` и `"всё"`, но **НЕ** матчит `"все"` (буква `е` ≠ `ё`).  

Когда regex пытается разобрать «все включено»:
- `вс` — ок
- `ё?` — пропускаем (optional)
- `\s*` — ожидаем пробел, но следующий символ `е` — **fail!**

Результат: `has_stars = False`, `has_meal = False` → `quality_check_passed = False` → блокировка.

---

### 2.2 ПРОБЛЕМА P2: Ложная блокировка + непрошеный upsell звёздности

**Сессия 6 (`99931e71`)**

Пользователь написал:
> «Хочу в Египет из Москвы, 5 марта, 7 ночей, двое взрослых, **четыре звезды, всё включено**»

**Что произошло:**
1. Модель вызвала `search_tours(stars: 4, meal: 7)` → каскад ЗАБЛОКИРОВАЛ
2. Ошибка: «Клиент НЕ указал категорию отеля (звёздность)»
3. Модель получив ошибку, ответила пользователю: **«Уточните, пожалуйста, хотите ли рассмотреть варианты с более высокой звёздностью, например, пятизвёздочные отели, или строго придерживаться четырёх звёзд?»**

**Корневая причина (двойная):**

**Баг A (тот же):** `stars_patterns` не распознаёт «четыре звезды» (слово + пробел).

**Баг B:** Каскад вернул ошибку с текстом `"Клиент НЕ указал категорию отеля (звёздность)"`. Модель, увидев эту ошибку, понимает, что пользователь УЖЕ говорил про 4 звезды, но система «не засчитала». Модель пытается «помочь» и интерпретирует ситуацию как неуверенность клиента — и предлагает повышение до 5 звёзд.

**Это самая вредная ошибка** — она:
- Раздражает клиента (он уже чётко сказал "четыре")
- Создаёт впечатление навязывания более дорогого варианта
- Добавляет лишний обмен сообщениями (увеличивает latency и стоимость)

---

### 2.3 ПРОБЛЕМА P3: Plaintext tool calls (yandexgpt/rc)

**Сессия 4 (`321bccff`)**

Пользователь написал: «Хочу тур в Сочи без перелёта»

**Что произошло:**
Модель вернула **текстовый ответ** вместо структурированного `function_call`:

```
Ответ 1: "```\nget_current_date()\n```"
Ответ 2: "```\nsearch_tours(country=47, regions=426, departure=99, ...)\n```"
```

Серверный лог (строки 228-241):
```
types=['message']  ← НЕ function_call!
"```\nget_current_date()\n```"
```

**Корневая причина:** Модель `yandexgpt/rc` **периодически** возвращает функциональные вызовы как обычный текст в формате markdown code block, а не как структурированные `function_call` объекты.

Это **интермиттентный баг** (не на каждом запросе). Доказательство: сессии 1, 2, 3, 5, 6, 7 использовали ту же модель и работали корректно.

**Последствия:**
- Пользователь видит сырой код функции вместо ответа
- Ни одна функция не выполняется
- Сессия полностью мёртвая

---

### 2.4 ПРОБЛЕМА P4: Параметр `country` vs `countries` в горящих турах

**Сессия 7 (`72180ccd`)**

Пользователь написал: «Что есть горящего из Москвы **в Турцию**?»

**Что произошло:**
- Модель вызвала: `get_hot_tours({"city": 1, "country": "4", "items": 10})`
- В `_dispatch_function` код использует `args.get("countries")` (множественное число)
- Модель передала `country` (единственное число)  
- Результат: `countries = None` → фильтр по стране **не применился**
- Пользователю показаны туры из России, Беларуси, Абхазии, Египта, Грузии, Азербайджана — **вместо только Турции**

**Корневая причина:** Несогласованность между schema (`countries` — plural) и тем, как модель иногда передаёт параметр (`country` — singular). Dispatch-функция не обрабатывает этот вариант.

---

### 2.5 ПРОБЛЕМА P5: API-таймаут get_tour_details

**Сессия 6 (`99931e71`), второй запрос: «Какие рейсы доступны?»**

```
get_tour_details → iserror: true → "Превышен лимит ожидания ответа от сервиса оператора"
```

**Это внешняя ошибка TourVisor API** — оператор не ответил за 11 секунд. Не баг нашей системы, но модель корректно обработала ситуацию, предложив повторить или выбрать другой вариант.

---

### 2.6 ПРИМЕЧАНИЕ: Empty response recovery (Сессия 7)

В сессии 7, при запросе `get_hotel_info`, API вернул пустой ответ (output_items=0). Система корректно это обработала:

```
⚠️ EMPTY RESPONSE #1 — falling back to full_history (11 items)
```

Откат на `full_history` сработал — следующий вызов вернул полную информацию об отеле.

---

## 3. АНАЛИЗ: ПОЧЕМУ АССИСТЕНТ «ПОВТОРЯЕТСЯ»

Прямая причина: **ложное срабатывание `_check_cascade_slots()`**.

Цепочка событий:
1. Клиент пишет «четыре звезды, все включено»
2. Модель **правильно** распознаёт → stars=4, meal=7 → вызывает `search_tours`
3. `_check_cascade_slots()` проверяет user_text по regex → **НЕ НАХОДИТ** «четыре звезды» (слово) и «все включено» (с "е")
4. Возвращает ошибку: «спроси клиента о звёздности/питании»
5. Модель вынуждена переспрашивать то, что уже известно

Это прямо нарушает правило §3.3.1 system_prompt: **«НЕ переспрашивай то, что уже известно»**.

---

## 4. АНАЛИЗ: ПОЧЕМУ АССИСТЕНТ ПРЕДЛАГАЕТ ЗАВЫСИТЬ ЗВЁЗДНОСТЬ

Цепочка событий (Сессия 6):
1. Ошибка каскада говорит: «Клиент НЕ указал звёздность»
2. Но модель помнит, что клиент сказал «четыре звезды»
3. Возникает конфликт: система говорит «не указал», но модель знает, что указал
4. Модель интерпретирует это как: «система хочет, чтобы я уточнил» → и задаёт вопрос
5. Поскольку модель «помогает», она расширяет вопрос: «может, хотите 5 звёзд?»

Дополнительный фактор: параметр `starsbetter=1` (по умолчанию) в API означает «показывать отели с категорией **выше** указанной». Модель знает об этом из schema и пытается «объяснить» клиенту, что в поиске всё равно появятся отели выше 4*.

**Это поведение недопустимо** — ассистент не должен навязывать повышение категории.

---

## 5. ЭКОНОМИЧЕСКИЙ АУДИТ

### 5.1 Тарификация

- **Модель:** `yandexgpt/rc` (Release Candidate)
- **Тарифы Yandex GPT** (2025/2026): ~0.40 ₽ / 1K токенов для актуальных моделей, ~2.40 ₽/1K для старых
- **Примем:** 0.40 ₽/1K токенов (RC-модель — экономичная)
- **Единица:** 1K токенов ≈ 3000-4000 символов русского текста

### 5.2 Размер базовых компонентов

| Компонент | Байты | Оценка токенов |
|-----------|-------|----------------|
| `system_prompt.md` | 132,906 | ~35,000 |
| `function_schemas.json` | 26,900 | ~9,000 |
| **Базовый overhead на вызов** | — | **~44,000** |

### 5.3 Стоимость по сессиям

С учётом `previous_response_id` (кэширование контекста на стороне Yandex), **полный overhead системного промпта платится только при первом вызове каждой сессии**. Последующие вызовы в сессии передают только дельту (~0.1-3K токенов), но Yandex всё равно тарифицирует обработку всего контекста.

| # | Session | YGP calls | Оценка входных токенов | Оценка выходных | Всего токенов | Стоимость (₽) |
|---|---------|-----------|----------------------|-----------------|---------------|---------------|
| 1 | `6dc27fd5` | 9 | ~55K | ~1K | ~56K | **~22 ₽** |
| 2 | `0d16df4b` | 7 | ~47K | ~1K | ~48K | **~19 ₽** |
| 3 | `48966250` | 2 | ~46K | ~0.5K | ~47K | **~19 ₽** |
| 4 | `321bccff` | 2 | ~46K | ~0.3K | ~46K | **~18 ₽** (ПОТЕРЯНО) |
| 5 | `b7f59db9` | 5 | ~50K | ~0.5K | ~51K | **~20 ₽** |
| 6 | `99931e71` | 9 | ~58K | ~1.5K | ~60K | **~24 ₽** |
| 7 | `72180ccd` | 10 | ~68K | ~3K | ~71K | **~28 ₽** |
| | **ИТОГО** | **44** | **~370K** | **~8K** | **~379K** | **~150 ₽** |

### 5.4 Где тратятся токены

**Главный потребитель — системный промпт (132 КБ, ~35K токенов):**

- При КАЖДОМ вызове Yandex GPT обрабатывает весь системный промпт
- 44 вызова × 35K = **~1.54M токенов** потрачено только на перечитывание промпта
- Это **~93% всех затрат** на токены

**Вторичные потребители:**
- Функциональные schema (~9K токенов на вызов) = ~5% затрат
- Диалоговая история + function results = ~2% затрат

### 5.5 Потери от багов

| Баг | Лишние API calls | Потерянные токены | Потерянные ₽ |
|-----|-----------------|-------------------|-------------|
| P1: Ложный каскад (Session 2) | +3 calls | ~15K | ~6 ₽ |
| P2: Ложный каскад + upsell (Session 6) | +3 calls | ~15K | ~6 ₽ |
| P3: Plaintext tool calls (Session 4) | 2 calls wasted | ~46K | ~18 ₽ (полная потеря) |
| **Итого потери** | **+8 calls** | **~76K** | **~30 ₽ (~20% бюджета)** |

### 5.6 Вызовы TourVisor API

TourVisor API тарифицируется по количеству поисков (search requests):

| Сессия | search.php | result.php (status) | result.php (results) | hottours.php | actdetail.php | hotel.php | Всего |
|--------|-----------|-------------------|---------------------|-------------|---------------|-----------|-------|
| 1 | 1 | 2 | 1 | — | — | — | 4 |
| 2 | 1 | 2 | 1 | — | — | — | 4 |
| 3 | — | — | — | 1 | — | — | 1 |
| 4 | — | — | — | — | — | — | **0** |
| 5 | 1 | 1 | 1 | — | — | — | 3 |
| 6 | 1 | 3 | 1 | — | 1 | — | 6 |
| 7 | 1 | 2 | 1 | 1 | — | 1 | 6 |
| **Итого** | **5** | **10** | **5** | **2** | **1** | **1** | **24** |

---

## 6. ИТОГОВАЯ ТАБЛИЦА НАЙДЕННЫХ БАГОВ

| ID | Серьёзность | Описание | Сессии | Корень |
|----|------------|----------|--------|--------|
| **P1** | СЕРЬЁЗНЫЙ | `stars_patterns` не распознаёт «пять/четыре звезд» (словесная форма) | 2, 6 | Regex в `_check_cascade_slots()` |
| **P2** | СЕРЬЁЗНЫЙ | `meal_patterns` не распознаёт «все включено» (с «е» вместо «ё») | 2 | Regex в `_check_cascade_slots()` |
| **P3** | КРИТИЧЕСКИЙ | Plaintext tool calls от `yandexgpt/rc` | 4 | Интермиттентный баг модели, нет safety net |
| **P4** | УМЕРЕННЫЙ | `country` vs `countries` в hot_tours dispatch | 7 | Нет fallback на singular форму |
| **P5** | ПОБОЧНЫЙ | Модель предлагает повысить звёздность при ложной блокировке | 6 | Следствие P1 + текст ошибки каскада |
| **P6** | ВНЕШНИЙ | Таймаут TourVisor API при get_tour_details | 6 | Внешний сервис, не наш баг |

---

## 7. ПЛАН ИСПРАВЛЕНИЙ (4 итерации)

### Итерация 1: Критическая стабилизация (Regex-баги)

**Файл:** `backend/yandex_handler.py`

**FIX-1A: Расширить `stars_patterns` для словесных числительных**

Текущий код (строки 204-207):
```python
stars_patterns = [
    r'\d\s*(?:звёзд|звезд|\*|⭐)',
    r'(?:пяти|четырёх|четырех|трёх|трех)звёзд',
]
```

Предлагаемый код:
```python
stars_patterns = [
    r'\d\s*(?:звёзд|звезд|\*|⭐)',
    r'(?:пяти|четырёх|четырех|трёх|трех)звёзд',
    r'(?:пять|четыре|три|два)\s+(?:звёзд|звезд)',   # "пять звезд", "четыре звезды"
]
```

**Риск регрессии:** МИНИМАЛЬНЫЙ. Добавляем новый паттерн, не меняя существующие.

---

**FIX-1B: Исправить `meal_patterns` для «все» с буквой «е»**

Текущий код (строка 209):
```python
r'(?:всё?\s*включен|all\s*incl|[ауа]и|ai\b|uai\b)'
```

Предлагаемый код:
```python
r'(?:вс[её]\s*включен|all\s*incl|[ауа]и|ai\b|uai\b)'
```

Изменение: `всё?` → `вс[её]` — теперь матчит и «все включено» (е), и «всё включено» (ё).

**Риск регрессии:** МИНИМАЛЬНЫЙ. Символьный класс `[её]` строже, чем `ё?` (который матчил «вс» без гласной). Это даже убирает ложноположительный матч на «вс включено» (без гласной).

---

**FIX-1C: Добавить «ультра все включено» в meal_patterns**

Добавить паттерн для ультра-варианта:
```python
r'(?:ультра\s*вс[её]\s*включен)',  # "ультра все включено"
```

**Риск регрессии:** НУЛЕВОЙ. Новый паттерн.

---

### Итерация 2: Safety net для plaintext tool calls

**Файл:** `backend/yandex_handler.py`

**FIX-2A: Детектор plaintext tool calls**

Добавить функцию `_extract_plaintext_tool_calls(text)` которая:
1. Ищет паттерны вида ` ```function_name(...)``` ` в тексте ответа
2. Парсит имя функции и аргументы
3. Возвращает список распознанных вызовов

Если обнаружен plaintext tool call:
1. **НЕ** показываем текст пользователю
2. Выполняем распознанные функции
3. Инжектим результаты в историю
4. Продолжаем цикл итераций

Это обеспечит работоспособность при интермиттентном баге `/rc` без отказа от экономичной модели.

**Риск регрессии:** НИЗКИЙ. Работает только когда ответ содержит паттерн ``` function_name( ```, что не встречается в нормальных текстовых ответах.

---

### Итерация 3: Фикс dispatch hot_tours

**Файл:** `backend/yandex_handler.py`

**FIX-3A: Fallback `country` → `countries`**

В `_dispatch_function`, блок `get_hot_tours`:
```python
countries=args.get("countries") or args.get("country"),
```

Если модель передала `country` (singular), используем его как `countries`.

**Риск регрессии:** НУЛЕВОЙ. Добавляет fallback, не ломает существующее поведение.

---

### Итерация 4: Улучшение текста ошибки каскада

**Файл:** `backend/yandex_handler.py`

**FIX-4A: Убрать провокацию upsell из текста ошибки**

Текущий текст ошибки:
> «ОБЯЗАТЕЛЬНО спроси клиента ЯВНО: Уточни у клиента: категорию отеля (звёздность, например: 4-5 звёзд)»

Текст «например: 4-5 звёзд» провоцирует модель предложить повышение. Заменить на:
> «Спроси клиента: Какую звёздность отеля предпочитаете? НЕ предлагай повышение!»

Добавить в текст ошибки явный запрет на upsell.

**Риск регрессии:** МИНИМАЛЬНЫЙ. Изменение только текста ошибки, не логики.

---

## 8. КАРТА РИСКОВ

| Фикс | Файл | Строки | Тип изменения | Риск |
|-------|------|--------|---------------|------|
| FIX-1A | yandex_handler.py | 204-207 | +1 regex паттерн | Минимальный |
| FIX-1B | yandex_handler.py | 209 | Замена `ё?` → `[её]` | Минимальный |
| FIX-1C | yandex_handler.py | 208-214 | +1 regex паттерн | Нулевой |
| FIX-2A | yandex_handler.py | Новая функция + вставка в chat()/chat_stream() | Новый код | Низкий |
| FIX-3A | yandex_handler.py | 1307 | +fallback `or` | Нулевой |
| FIX-4A | yandex_handler.py | тексты ошибок | Замена текста | Минимальный |

---

## 9. РЕКОМЕНДАЦИИ ПО ОПТИМИЗАЦИИ СТОИМОСТИ

1. **Системный промпт (133 КБ)** — главный потребитель токенов (~93%). Если сократить его на 30%, экономия составит ~10 ₽ на тестовую сессию из 7 диалогов. В продакшене при 100 диалогах/день — ~150 ₽/день.

2. **Ложные блокировки каскада** — каждая стоит ~6 ₽ (3 лишних API-вызова). Исправление P1+P2 полностью устранит эти потери.

3. **Plaintext tool call failures** — полная потеря сессии (~18 ₽). Safety net (FIX-2A) превратит потерю в штатную работу.

4. **Модель `yandexgpt/rc`** — оправданный выбор для экономии. Из 7 сессий plaintext-баг проявился только в 1 (14%). Safety net решит проблему.

---

## 10. ЗАКЛЮЧЕНИЕ

**Главная проблема** — не модель, а **regex-валидация каскада**. Два паттерна (`stars_patterns` и `meal_patterns`) слишком узкие, из-за чего:
- Клиент вынужден повторять уже сказанное
- Ассистент навязывает повышение звёздности
- Теряется ~20% бюджета на лишние API-вызовы

**Приоритет фиксов:**
1. **НЕМЕДЛЕННО:** FIX-1A + FIX-1B + FIX-1C (regex) — устраняют 2 из 3 проблемных сессий
2. **ВАЖНО:** FIX-2A (safety net) — страховка от plaintext-бага `/rc`
3. **ПОЛЕЗНО:** FIX-3A + FIX-4A — мелкие улучшения

**Жду вашего решения о внесении правок.**
