# ВЕРИФИЦИРОВАННЫЙ АНАЛИЗ ЛОГОВ 10 ФЕВРАЛЯ 2026
## AI-Турменеджер МГП — Глубокий анализ с проверкой на регрессию

**Дата анализа:** 10.02.2026  
**Методология:** Каждая сессия прослежена строка-за-строкой через код `yandex_handler.py`, `app.py`, `system_prompt.md`, `function_schemas.json` и `.env`.

---

## ЧАСТЬ 1: ОБЩАЯ КАРТИНА

### Результат тестирования

| # | Сессия | Запрос | Исход | Причина смерти |
|---|--------|--------|-------|----------------|
| 1 | d42afd4a | Rixos в Турции, без перелёта | ❌ МЕРТВА на 3-м ходе | Plaintext tool call |
| 2 | f0a5b2da | Турция, НН, 5★ AI, ребёнок 9 лет | ⚠️ Результат из НЕВЕРНОГО города | departure=9 (Новосибирск) вместо Н.Новгорода |
| 3 | 2b3b82ff | Египет из Москвы, 2 взр + 2 детей | ❌ МЕРТВА на 1-м ходе | Plaintext tool call |
| 4 | 9143b3d7 | Египет/ОАЭ из Тюмени/Екб | ❌ МЕРТВА на 2-м ходе | Ложь + plaintext tool call |

**3 из 4 сессий убиты. 1 сессия — результат с критической ошибкой.**

---

## ЧАСТЬ 2: КОРНЕВЫЕ ПРИЧИНЫ (ГЛУБОКИЙ АНАЛИЗ)

### ПРОБЛЕМА #1 (КРИТИЧЕСКАЯ): Модель `yandexgpt/rc` — plaintext tool calls

**Что происходит:** Yandex GPT API возвращает `types=['message']` вместо `types=['function_call']`. Содержимое — текст с синтаксисом вызовов:
- `[TOOL_CALL_START]search_tours\n{"adults":2,...}` (сессии 1, 4)
- ` ```\nget_current_date()\n``` ` (сессия 3)

**Почему это не ловится:**
1. `_is_promised_search()` (строки 53-85) ищет ФРАЗЫ типа «сейчас поищу», а не синтаксис `[TOOL_CALL_START]`
2. В коде `chat()` (строки 1538-1613) ветка `else:` для текста просто извлекает `final_text` и возвращает его
3. `app.py` (строки 316-347) передаёт `reply` фронтенду БЕЗ фильтрации
4. Фронтенд показывает сырой текст пользователю

**Ключевое открытие:** В `.env` стоит `YANDEX_MODEL=yandexgpt/rc`. В логах 7-9 февраля использовалась `yandexgpt` (без `/rc`). **Смена модели — первопричина.**

**Верификация:** Серверный лог подтверждает:
```
🤖 YandexGPTHandler INIT  model=gpt://b1gl8hejnsdm5lip6m7k/yandexgpt/rc
```

Модель URI формируется в `yandex_handler.py:485`: `self.model_uri = f"gpt://{self.folder_id}/{self.model}"`.

---

### ПРОБЛЕМА #2: Каскадный валидатор — regex баги

Прослежено через код `_check_cascade_slots()` (строки 88-274):

#### 2a. «без перелёта» не распознаётся как departure

**Трассировка (сессия 1):**
- user_text = `"нужен отель в турции rixos без перелета"`
- Перебираю `departure_patterns` (строки 115-141): 17 паттернов городов + 2 обобщённых
- Ни один НЕ содержит `без\s*перел[её]т`
- `has_departure_mention = False` → missing.append("город вылета")

**Причина:** `departure_patterns` (строки 115-141) не знают про `departure=99` (без перелёта), хотя `_DEPARTURE_VALIDATION` (строка 318) знает: `(r'без\s*перел[её]т\w*', 99)`.

#### 2b. «все включено» (с буквой «е») не матчится

**Трассировка (сессия 2):**
- user_text содержит `"все включено"`
- Паттерн `meal_patterns[0]` = `r'(?:всё?\s*включен|...)'`
- Regex `всё?` разбивается так: литерал `в` → литерал `с` → литерал `ё` (0 или 1 раз)
- Матчит: `"вс"` и `"всё"`, НО НЕ `"все"` (потому что `е` ≠ `ё`)
- Проверка: `всё?` на `"все включено"` → "вс" найдено на позиции 0-1, ё? = 0 раз → позиция 2 = `"е"` → `\s*` не матчит `"е"` → `"включен"` не начинается → **FAIL**

#### 2c. «пять звезд» (словом) не матчится

**Трассировка (сессия 2):**
- user_text содержит `"пять звезд"`
- `stars_patterns[0]` = `r'\d\s*(?:звёзд|звезд|\*|⭐)'` → требует ЦИФРУ → `"пять"` не цифра → **FAIL**
- `stars_patterns[1]` = `r'(?:пяти|четырёх|...)звёзд'` → требует СЛИТНОЕ написание `"пятизвёзд"` → **FAIL**

**Следствие:** `has_stars = False`, `has_meal = False` → `quality_check_passed = False` → каскад блокирует и спрашивает QC → ЛИШНИЙ вопрос → пользователь вынужден подтверждать «да».

---

### ПРОБЛЕМА #3: Неверный город вылета — «ННовгорода» → Новосибирск

**Трассировка (сессия 2):**

1. **Модель выбрала departure=9:** Пользователь написал «ННовгорода». В system_prompt (раздел 6.2, строки 722-738) НЕТ Нижнего Новгорода — только 13 городов. Модель увидела «Нов...» и угадала Новосибирск (id=9).

2. **_DEPARTURE_VALIDATION не поправил:** user_text = `"вылет с нновгорода"`. Перебираю все 15 паттернов `_DEPARTURE_VALIDATION` (строки 302-319):
   - `r'(?:из|с)\s+москв\w*'` → «с нновгорода» ≠ «с москв*» → NO
   - ... все 15 паттернов → NONE match «с нновгорода»
   - Результат: departure=9 проходит без коррекции

3. **Каскадный валидатор НЕ заблокировал:** Паттерн `r'(?:вылет|вылетаем|летим|улетаем)\s+(?:из|с)\s+\w+'` (строка 139) матчит `"вылет с нновгорода"` → `has_departure_mention = True` → каскад пропускает

4. **Итог:** Поиск идёт из Новосибирска, карточки показывают «Из: Новосибирск», пользователь получает нерелевантные результаты.

---

### ПРОБЛЕМА #4: Системный промпт содержит ложную информацию о Тюмени

**Трассировка (сессия 4):**

1. **Модель НЕ вызвала API.** Модель ответила текстом: «К сожалению, вылеты из Тюмени в нашей базе недоступны.»
   - Серверный лог: `types=['message']` — ни одного function_call, чисто текст
   - Это значит модель **следует инструкции из промпта**

2. **Проверяю system_prompt.md, строка 536:**
   ```
   «Из Тюмени» | Тюмени нет в базе → сообщи и предложи альтернативы: Екатеринбург, Москва, Новосибирск
   ```
   **ПРОМПТ ЯВНО ГОВОРИТ МОДЕЛИ, ЧТО ТЮМЕНЬ НЕДОСТУПНА!**

3. **Но в `_check_cascade_slots` (строка 130):** `r'\b(?:тюмен[ьи])\b'` — каскадный валидатор знает про Тюмень. Противоречие.

4. **Реальность:** TourVisor API, вероятно, ПОДДЕРЖИВАЕТ Тюмень. Нужна верификация через `get_dictionaries(type=departure)`.

---

### ПРОБЛЕМА #5 (НАЙДЕНА ПРИ ПЕРЕПРОВЕРКЕ): Нижний Новгород отсутствует в system_prompt

**Дополнительная находка:** Раздел 6.2 system_prompt (строки 722-738) — таблица городов вылета:
```
Москва=1, Пермь=2, Екатеринбург=3, Уфа=4, СПб=5, Челябинск=6, Самара=7,
Новосибирск=9, Казань=10, Краснодар=11, Красноярск=12, Ростов=18, Сочи=56, Без перелёта=99
```

**Нижнего Новгорода НЕТ.** Модель не знает его departure ID → вынуждена угадывать → угадала неправильно (Новосибирск).

---

## ЧАСТЬ 3: УТОЧНЁННЫЙ КАТАЛОГ ВСЕХ ПРОБЛЕМ

| # | Проблема | Уровень | Где в коде | Сессии |
|---|----------|---------|------------|--------|
| P1 | Модель `yandexgpt/rc` возвращает function calls как текст | **КРИТИЧЕСКИЙ** | `.env:9` + `yandex_handler.py:1538-1613` | 1, 3, 4 |
| P2 | «без перелёта» отсутствует в `departure_patterns` каскада | СЕРЬЁЗНЫЙ | `yandex_handler.py:115-141` | 1 |
| P3 | «все включено» (с «е») не матчится в `meal_patterns` | СЕРЬЁЗНЫЙ | `yandex_handler.py:209` | 2 |
| P4 | «пять звезд» (словом) не матчится в `stars_patterns` | СЕРЬЁЗНЫЙ | `yandex_handler.py:204-207` | 2 |
| P5 | Нижний Новгород отсутствует в system_prompt + `_DEPARTURE_CITIES` + `_DEPARTURE_VALIDATION` | СЕРЬЁЗНЫЙ | `system_prompt.md:722-738`, `yandex_handler.py:292-319` | 2 |
| P6 | system_prompt ложно утверждает «Тюмень не в базе» | СЕРЬЁЗНЫЙ | `system_prompt.md:536` | 4 |
| P7 | `_DEPARTURE_VALIDATION` не содержит паттерн для «ннов*»/«н.новгород» | СЕРЬЁЗНЫЙ | `yandex_handler.py:302-319` | 2 |
| P8 | Cascade `departure_patterns` не содержит `ннов*` | УМЕРЕННЫЙ | `yandex_handler.py:131` | 2 |

---

## ЧАСТЬ 4: ПЛАН ИСПРАВЛЕНИЙ С ПРОВЕРКОЙ НА РЕГРЕССИЮ

### Итерация 0: Экстренная стабилизация (МОДЕЛЬ)

#### FIX-0A: Откат модели на `yandexgpt` (НАИМЕНЬШИЙ РИСК)

**Файл:** `.env`, строка 9  
**Изменение:** `YANDEX_MODEL=yandexgpt/rc` → `YANDEX_MODEL=yandexgpt`

**Проверка на регрессию:**
- ✅ Модель `yandexgpt` работала корректно 7-9 февраля (function calls как function_call, не текст)
- ⚠️ `/rc` мог содержать улучшения в качестве ответов/понимании контекста
- ⚠️ Без `/rc` могут вернуться проблемы из логов 7-9 февраля (но те были менее критичны)
- **Вердикт: БЕЗОПАСНО.** Откат к проверенной рабочей версии.

#### FIX-0B: Safety-net — детектор plaintext tool calls (СТРАХОВКА)

**Файл:** `yandex_handler.py`  
**Где:** Новая функция + интеграция в `chat()` между строками 1540-1602

**Логика:**
1. Если `final_text` содержит `[TOOL_CALL_START]` или ` ```func_name()``` `:
   - Извлечь имя функции + аргументы
   - Выполнить функции программно
   - НЕ сохранять текст в full_history (он мусорный)
   - Сохранить результаты функций в full_history
   - Сбросить `previous_response_id` (ответ с текстом бесполезен)
   - Установить `self.input_list = list(self.full_history)`
   - Продолжить цикл (модель получит результаты и ответит нормально)
2. Счётчик: максимум 2 попытки, затем отдаём текст как есть

**Проверка на регрессию:**
- ⚠️ **Ложные срабатывания:** `[TOOL_CALL_START]` — синтетический маркер Yandex API, не встречается в естественном тексте. Markdown code blocks с именами функций теоретически возможны, но системный промпт запрещает генерацию кода (раздел 27.1). **РИСК: МИНИМАЛЬНЫЙ**
- ⚠️ **Ошибка парсинга JSON:** Аргументы могут быть обрезаны (сессия 1: JSON обрезан в середине). **НЕОБХОДИМ try/except** — при ошибке парсинга пропускаем и отдаём текст
- ⚠️ **Бесконечный цикл:** Модель на retry может снова вернуть plaintext. **Необходим счётчик** (max 2)
- ⚠️ **Потеря previous_response_id:** Сброс → потеря контекста. **НО** full_history содержит все данные, и существующий fallback-механизм (строки 1489-1492) уже работает так же
- **Вердикт: БЕЗОПАСНО при наличии try/except + счётчика + проверки перед `_is_promised_search`**

**Порядок детекции в chat() (КРИТИЧЕСКИ ВАЖНО):**
```
1. Пустой ответ? → fallback
2. Самомодерация? → retry
3. >>> PLAINTEXT TOOL CALL? → extract + execute + retry <<<  (НОВОЕ)
4. Promised search? → nudge
5. Dedup → return
```
Если поставить ПОСЛЕ `_is_promised_search` — конфликта не будет, но `_is_promised_search` не поймает эти паттерны (она ищет фразы, не синтаксис). Если поставить ПЕРЕД — тоже OK, потому что `_is_promised_search` не имеет пересечения паттернов.

---

### Итерация 1: Фиксы каскадного валидатора (REGEX)

#### FIX-1A: «без перелёта» в departure_patterns

**Файл:** `yandex_handler.py`, после строки 136  
**Добавить:**
```python
r'(?:без\s*перел[её]т\w*)',   # departure=99
```

**Проверка на регрессию:**
- ✅ Паттерн `без\s*перел[её]т\w*` специфичен — матчит только «без перелёта/перелёт/перелётов»
- ✅ Не конфликтует с другими паттернами
- ✅ Не изменяет существующие матчи
- **Вердикт: ПОЛНОСТЬЮ БЕЗОПАСНО**

#### FIX-1B: «все/всё включено» в meal_patterns

**Файл:** `yandex_handler.py`, строка 209  
**Изменение:**
```
Было:  r'(?:всё?\s*включен|all\s*incl|[ауа]и|ai\b|uai\b)'
Стало: r'(?:вс[её]\s*включен|all\s*incl\w*|[ауа]и|ai\b|uai\b)'
```

**Проверка на регрессию:**
- Было `всё?` → матчит "вс" и "всё"
- Стало `вс[её]` → матчит "все" и "всё"
- ⚠️ Потеряли матч "вс" (без буквы е/ё) — но "вс включено" в реальности не встречается, это не слово
- ✅ Добавили матч "все" — самый частый вариант в русском тексте
- ✅ `all\s*incl\w*` вместо `all\s*incl` — теперь матчит "all inclusive" полностью
- **Вердикт: БЕЗОПАСНО. Потеря "вс" — несуществующий edge case.**

#### FIX-1C: «пять/четыре/три звезд» (словом)

**Файл:** `yandex_handler.py`, строки 204-207  
**Добавить третий паттерн:**
```python
stars_patterns = [
    r'\d\s*(?:звёзд|звезд|\*|⭐)',
    r'(?:пяти|четырёх|четырех|трёх|трех)\s*звёзд',  # + \s* для раздельного
    r'(?:пять|четыре|три)\s+(?:звёзд|звезд)',        # НОВЫЙ: словами
]
```

**Проверка на регрессию:**
- ✅ Существующие паттерны не изменены (только добавлен `\s*` во второй)
- ✅ Новый паттерн матчит «пять звезд», «четыре звезды», «три звезды»
- ⚠️ Может ли «пять» матчить ложно? В контексте тур-запроса «пять звезд» всегда означает 5★
- ⚠️ Второй паттерн: добавление `\s*` расширяет «пятизвёзд» до «пяти звёзд» (раздельно) — это корректное расширение
- **Вердикт: БЕЗОПАСНО**

---

### Итерация 2: Данные городов (ТРЕБУЕТ ВЕРИФИКАЦИИ)

#### FIX-2A: Верификация departure IDs через API

**ПРЕЖДЕ ЧЕМ вносить правки**, необходимо вызвать `get_dictionaries(type=departure)` и получить:
1. Есть ли Нижний Новгород в API? Если да — какой ID?
2. Есть ли Тюмень в API? Если да — какой ID?
3. Какие ещё города есть, но отсутствуют в промпте/коде?

**Это БЛОКИРУЮЩИЙ шаг для FIX-2B, 2C, 2D.**

#### FIX-2B: Добавить Нижний Новгород (после верификации ID)

**Файлы:**
1. `system_prompt.md` (строки 722-738): добавить строку в таблицу 6.2
2. `yandex_handler.py:292-297` (`_DEPARTURE_CITIES`): добавить `{ID}: "Нижний Новгород"`
3. `yandex_handler.py:302-319` (`_DEPARTURE_VALIDATION`): добавить паттерн:
   ```python
   (r'(?:из|с)\s+(?:нижн\w+\s*новгород|н[\.\s]*новгород)\w*', ID),
   (r'\b(?:ннов\w*|н[\.\s]*новгород)\b', ID),  # ННовгород, Н.Новгород
   ```
4. `yandex_handler.py:131` (`departure_patterns`): расширить:
   ```python
   r'\b(?:нижн\w+\s*новгород|нижний|ннов\w*|н[\.\s]*новгород)\b',
   ```

**Проверка на регрессию:**
- ⚠️ **ID ДОЛЖЕН БЫТЬ ВЕРИФИЦИРОВАН** — если вставить неверный ID, все запросы из Н.Новгорода пойдут в неверный город
- ⚠️ Паттерн `ннов\w*` — может ли матчить что-то кроме «нновгород»? В контексте тур-запросов — нет, «ннов» не является частью других слов
- ✅ Добавление в промпт — безопасно, модель получит информацию
- **Вердикт: БЕЗОПАСНО, НО ТОЛЬКО ПОСЛЕ ВЕРИФИКАЦИИ ID**

#### FIX-2C: Исправить информацию о Тюмени в промпте (после верификации)

**Файл:** `system_prompt.md`, строка 536

**Изменение:** Если Тюмень ЕСТЬ в API:
```
Было:  «Из Тюмени» | Тюмени нет в базе → ...
Стало: «Из Тюмени» | departure=ID (проверить через get_dictionaries если не уверен)
```

Плюс добавить в таблицу 6.2 и в `_DEPARTURE_CITIES`/`_DEPARTURE_VALIDATION`.

Если Тюмени НЕТ в API — оставить промпт как есть.

**Проверка на регрессию:**
- ✅ Если Тюмень действительно есть — правка промпта улучшит результаты
- ⚠️ Если Тюмени нет — любые правки в промпте будут вредны
- **Вердикт: БЕЗОПАСНО ТОЛЬКО ПОСЛЕ ПОДТВЕРЖДЕНИЯ ЧЕРЕЗ API**

#### FIX-2D: Расширить `_DEPARTURE_CITIES` полным списком из API

**Файл:** `yandex_handler.py:292-297`

**Проверка на регрессию:**
- ⚠️ Если добавить неверные маппинги → departure mismatch для множества городов
- ✅ Если данные из API корректны → все города валидируются правильно
- **Вердикт: СРЕДНИЙ РИСК — нужна тщательная верификация маппингов**

---

### Итерация 3: Защитные улучшения (ОПЦИОНАЛЬНО)

#### FIX-3A: Добавить «ннов*» в cascade departure_patterns

Уже включено в FIX-2B. Отдельно не требуется.

---

## ЧАСТЬ 5: КРИТИЧЕСКИЙ АНАЛИЗ ПЕРВОНАЧАЛЬНЫХ ПРЕДЛОЖЕНИЙ

### Что я предлагал НЕПРАВИЛЬНО или НЕТОЧНО в первом отчёте:

| Предложение | Проблема | Исправление |
|-------------|----------|-------------|
| FIX-T6: «Перенести «без перелёта» в НАЧАЛО _DEPARTURE_VALIDATION» | **РИСК РЕГРЕССИИ!** Если пользователь сначала сказал «без перелёта», а потом передумал и попросил вылет из Москвы — валидатор всё равно будет выбирать departure=99, потому что «без перелёта» в combined user_text матчит первым. | **ОТМЕНЯЮ FIX-T6.** Правильный подход — FIX-1A (добавить в cascade departure_patterns). Тогда каскад не будет спрашивать город вылета лишний раз, и конфликт не возникнет. |
| FIX-T5: «Добавить Нижний Новгород ID=13» | **Я УГАДАЛ ID!** Я написал «ID нужно уточнить через get_dictionaries(type=departure)», но в таблице указал `13` — это НЕПРОВЕРЕННОЕ значение. | **Добавил блокирующий шаг FIX-2A** — сначала верификация через API. |
| FIX-T7: «Полный кэш городов из API при запуске сервера» | Слишком амбициозно для первой итерации. Загрузка всех справочников при старте — изменение архитектуры. | **Перенёс в «опционально»** — сначала точечные фиксы. |
| FIX-T1: «Парсить plaintext и выполнять» | Предложение было верным по сути, НО я не учёл: (1) обрезанные JSON в логах (сессия 1), (2) необходимость сброса previous_response_id, (3) fallback через full_history. | **Уточнил FIX-0B** с правильной механикой retry. |

---

## ЧАСТЬ 6: РЕКОМЕНДОВАННЫЙ ПОРЯДОК ДЕЙСТВИЙ

### Фаза 1: НЕМЕДЛЕННО (минимальный риск, максимальный эффект)

| # | Действие | Файл | Риск | Восстанавливает |
|---|----------|------|------|-----------------|
| FIX-0A | Откат `YANDEX_MODEL=yandexgpt` | `.env` | НУЛЕВОЙ | 75% убитых сессий |
| FIX-1A | «без перелёта» в cascade departure | `yandex_handler.py` | МИНИМАЛЬНЫЙ | Сессию 1 |
| FIX-1B | `вс[её]` вместо `всё?` в meal | `yandex_handler.py` | МИНИМАЛЬНЫЙ | Лишние QC-вопросы |
| FIX-1C | «пять/четыре/три звезд» словами | `yandex_handler.py` | МИНИМАЛЬНЫЙ | Лишние QC-вопросы |

**Ожидаемый результат:** 4 из 4 сессий сегодня прошли бы корректнее. Сессии 1, 3, 4 ожили бы. Сессия 2 всё ещё имела бы проблему с departure (нужна фаза 2).

### Фаза 2: ПОСЛЕ ВЕРИФИКАЦИИ API

| # | Действие | Файл | Риск | Восстанавливает |
|---|----------|------|------|-----------------|
| FIX-2A | Вызвать `get_dictionaries(type=departure)` | — | НУЛЕВОЙ | Данные для фиксов |
| FIX-2B | Добавить Нижний Новгород (если в API) | `system_prompt.md` + `yandex_handler.py` | НИЗКИЙ | Сессию 2 |
| FIX-2C | Исправить Тюмень в промпте (если в API) | `system_prompt.md` + `yandex_handler.py` | НИЗКИЙ | Сессию 4 |

### Фаза 3: СТРАХОВКА (после тестирования фаз 1-2)

| # | Действие | Файл | Риск | Восстанавливает |
|---|----------|------|------|-----------------|
| FIX-0B | Plaintext tool call detector | `yandex_handler.py` | НИЗКИЙ-СРЕДНИЙ | Защита от любых моделей |

---

## ЧАСТЬ 7: ЧТО Я МОГ ПРОПУСТИТЬ — ЧЕСТНЫЙ САМОАНАЛИЗ

| Вопрос | Ответ |
|--------|-------|
| Мог ли `yandexgpt/rc` быть выбран намеренно? | Да. Возможно, `/rc` лучше понимает контекст или быстрее. Откат решит plaintext-проблему, но может ухудшить качество ответов. |
| Есть ли другие сломанные regex в каскаде? | Проверил все 4 блока (departure, dates, nights, travelers, QC). Найденные — описаны выше. Travelers работают корректно («один взрослый» матчится через `(?:один|одна|сам|одиночк)`). Dates работают корректно («14 , 15, 16 июля» → `16 июля` матчится). |
| Может ли FIX-0B (plaintext detector) ухудшить нормальные ответы? | Если модель вернёт легитимный текст с `[TOOL_CALL_START]` — ложное срабатывание. Но это синтетический маркер Yandex API, не встречающийся в естественном русском тексте. Риск минимален. |
| Проверил ли я streaming (chat_stream)? | Да. Streaming парсит function_call через `response.output_item.done` с `item_type == 'function_call'`. Та же проблема: если модель вернёт plaintext в streaming — текст пройдёт через `response.output_text.delta` без детекции. FIX-0B нужно также реализовать в `chat_stream()`. |
| Не сломают ли regex-фиксы существующие матчи? | FIX-1A: добавление нового паттерна — не меняет существующие. FIX-1B: `вс[её]` vs `всё?` — потеря «вс» (без е/ё) — нереальный случай. FIX-1C: добавление нового паттерна — не меняет существующие. |
| Длительность ночей в сессии 2 не уточнена | Пользователь дал 3 даты (14, 15, 16 июля) без длительности. Модель использовала дефолт 7-10. Каскад не блокирует (by design — строка 177). Это допустимое поведение, НО в промпте (строка 308) сказано: «Если указал ТОЛЬКО дату/месяц без длительности → спроси длительность». Однако пользователь дал конкретные даты вылета, и каскад считает это достаточным. **Мнение: пограничный случай, не критично.** |
